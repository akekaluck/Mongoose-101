# Mongoose-101
1. connect
  * create connection
    - conn = mongoose.connect
    - conn = mongoose.createConnection
  * create schema
    - mongoose.Schema
  * create model
    - conn.model
2. find
3. populate
4. $lookup
  - Lookup data to another collection
